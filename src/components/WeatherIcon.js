import React from "react";

const WeatherIcon = ({ icon }) => {
  // You can use an icon library or custom SVGs here
  return <div className="weather-icon">{icon}</div>
};

export default WeatherIcon;
